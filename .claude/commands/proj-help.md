# `/proj-help`

直接执行本地帮助命令。

## Execution

When this command is invoked, run:

```bash
tools/proj_help.sh
```

Return the command output directly. Do not restate the command documentation unless the user asks for explanation.
