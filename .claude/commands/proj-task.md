# `/proj-task`

直接查看或更新当前 task board。

## Execution

For listing:

```bash
tools/proj_task.sh --list
```

For updates:

```bash
tools/proj_task.sh <args>
```

Return the helper output directly. If a task or checkpoint status changes, mention the updated id and new status.
