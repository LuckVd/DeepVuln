# Proj-Check Workflow

1. Read `CONSTITUTION.md`.
2. Read the latest change intent.
3. Run protection, scope, goal, task-board, cleanup, and structure-sync checks.
4. Run API review when API-facing files changed.
5. Write `docs/reports/check-latest.md` as a derived check record.
6. Return a short structured summary with failing gates and next actions.
