# Proj-Task Workflow

1. Read `CURRENT_TASKS.yaml`.
2. If needed, list the current tasks and checkpoints to discover ids.
3. Update the target task or checkpoint state.
4. Record `status_note`, `status_updated_at`, and `status_updated_by` when provided.
5. Refresh the task-board timestamp.
