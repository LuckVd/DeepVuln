goal_id: "phase18-precision-link-reachability"
title: "Phase 18 — 精度链路接通与多语言可达性补齐（基于全量实现审查）"
stage: "active"
status: "pending"
design_confirmed: true
blocked_by: []
open_questions:
  - "C/C++ 支持已确认放弃（用户 2026-06-20）；AST supported_languages 需收敛并显式标 c/cpp 不支持"
  - "安全组（IDOR/WS认证/源路径白名单/弱口令）已确认降级暂缓（单用户内网）；仅做轻量加固，待公网/多用户时整体补多租户"
  - "对抗增强层(~2000行 enhanced/convergence/strategy_library/gatekeeper)：P6 设计时定 gatekeeper 接线 vs 整体删除"
resolved_questions:
  - "全量审查(2026-06-20,8路并行,12万行)确认 Phase 17 多项'声称完成'实际未生效：四轮只跑Round1/裁决映射属性名错confidence恒0/D5半成品/CPG可达性JS-Java-Go空/D3续扫失效/pause假停/增强层死代码"
  - "Java call_graph builder(java_builder.py 395行)已完整实现，仅 analyzer.py:55 未注册 → Java可达性接通近乎免费(一行注册)"
  - "Go call_graph builder 文件不存在 + GoCPGProvider 注释成TODO → Go可达性需新建 builder+provider(中等工程)"
  - "语言收窄 py/go/java，放弃 C/C++；安全组降级（用户 2026-06-20 确认场景）"
implementation_steps:
  - "P0 精度链路(A1+A2+A3): Round2/3补add_candidate回填; scan_orchestrator:1377,1381改读candidate.finding.*; D5同步finding.confidence。工作量S [第一批]"
  - "P1 Java可达性(A4-Java): analyzer.py:55注册JavaCallGraphBuilder(已实现)。工作量S [第一批]"
  - "P4 语言收敛+CodeQL类型(C1+C2): AST supported_languages收敛py/go/java(不支持返skipped); 修executor.py:332类型混淆。工作量S [第一批]"
  - "P3 可达性质量(A4): identify_basic_blocks递归进if/for/while体; 兜底默认True改保守。工作量M [第二批]"
  - "P2 Go可达性(A4-Go): 新建go_builder.py(仿java)+GoCPGProvider注册。工作量M [第二批]"
  - "P5 续扫完整(A5+A6): 统一ScanPhase命名; resume恢复实例状态(tech_stack等); 落库去重upsert; pause接revoke。工作量M-L [第三批]"
  - "P7 可靠性(C3-C7): git clone超时; baseline覆盖判定; 增量import拼接; 引擎级checkpoint; budget排序; 清死代码。工作量M [第三批]"
  - "P6 对抗接线(A7): gatekeeper替换手写版; min_evidence_dimensions提升; 修LIKELY->MEDIUM; 定enhanced层去留。工作量M [第三批]"
  - "P8 测试真实性(E,横切): 每修一处配真实测试; 删/修8个async空转测试; 修5个gatekeeper疑似bug; 修path_provider矛盾。工作量M [横穿]"
  - "P9(降级) 安全暂缓+轻量加固: 改默认口令; get_db加rollback; repository不内部commit。工作量S(加固部分) [暂缓]"
test_scope:
  - "P0: Round2/3回填candidate单测 + 裁决mapping属性单测 + confidence不再恒0断言"
  - "P1: Java真源码 attack-path 端到端(可达/不可达) — 不再恒空"
  - "P2: Go真源码 attack-path 端到端"
  - "P3: 嵌套sink(if/for体内)恒可达修复的负样本 + if False:eval 判False"
  - "横切: D3 Celery/E5 GLM/checkpoint/CodeQL mock 测试升级为真实或如实标注"
test_result: "第三批 P5 续扫完整性 + P7 可靠性(C3/C4/C5/C7/死代码)已完成并 push(a9ef21a + bc1db1f,本地=origin=bc1db1f)。P5: H2a pause_scan 接 revoke + H2b clone kill_after_timeout(查证 GitPython 用 kill_after_timeout 非 timeout)+ task6 _finalize_results DB 去重(resume 防翻倍)+ task7 阶段命名统一收敛 ScanPhase(SCAN_PHASE_ORDER+_LEGACY_PHASE_ALIASES 单一事实源,get_resume_strategy 修复 resume 全量重跑根因,PhaseManager/_create_initial_phases 改10阶段消除 scan_phases 双套行,broadcaster 补 logic_vuln_discovery 权重,PhaseName deprecation)。P7: C3 git timeout + C4 baseline fixed 加 scanned_files 覆盖判定(防误报已修复)+ C5 增量 relative import level 解析(防漏扫 dependent)+ C7 finding_budget 截断前按 final_score/confidence+severity 排序(防随机丢高分)+ 删死代码 _deduplicate_findings/calculate_finding_confidence。P8: 3 个 web 测试文件 fixture 修复(patch 不存在的 AsyncSessionLocal→get_session_local,从 setup ERROR→全绿)+ 删 4 个 CLIAdapter 死测试(CLI 已移除)。验证: 全 test_l3 2200 passed/23 既有失败(零回归,+10 新真实测试)。P6 对抗接线整体留独立会话(用户决定),子项评估: 子项3 round_three LIKELY→MEDIUM 一行低风险;子项4 enhanced 1926行死代码确认只 verification/__init__.py re-export 无功能引用可删;子项2 min_evidence_dimensions 1→2 精度/召回权衡(本机无 CodeQL→仅 taint+reachability 达2,改后单硬证据真漏洞可能漏报);子项1 gatekeeper 接线但 test_verification_gatekeeper 有5个既有失败需先修 gatekeeper。剩: P6(独立会话)/ C6 引擎级checkpoint(并发+per-engine checkpoint,M-L)/ P3 可达性质量(需条件求值,深做前先设计,兜底改保守有回归风险)。"
new_files:
  - "src/layers/l3_analysis/call_graph/builders/go_builder.py (P2-Go, 已实施 4b15224)"
  - "src/layers/l3_analysis/engines/ast_engine/cpg/providers/go_provider.py (P2-Go, 已实施 4b15224)"
modified_files:
  - "第一批(4ee490b): rounds/round_two.py,round_three.py / src/web/services/scan_orchestrator.py / rounds/round_four.py / scoring / call_graph/analyzer.py / engines/ast_engine/ast_engine.py / engines/codeql/executor.py"
  - "第二批(6b0f290+4b15224): engines/ast_engine/cpg/models.py / call_graph/builders/base.py / path_finder/finder.py + go_builder.py(新)/cpg/providers/go_provider.py(新)/cpg/path_provider.py / call_graph/analyzer.py"
  - "P6-eval(4409eb4): rounds/round_four.py"
security_blockers:
  - "安全组降级(单用户内网,用户2026-06-20确认): IDOR无owner / WS无认证 / 源路径无白名单 / auth/seed未授权+弱口令 / 限速默认关 / APIkey明文 / CSV注入 / git注入 / JWT无失效 — 暂缓,待公网/多用户时整体补多租户"
dead_code_actions:
  - "A7 对抗增强层(~2000行 enhanced_adversarial/convergence/strategy_library/gatekeeper): P6 定 gatekeeper 接线 或 删除"
  - "_deduplicate_findings(orchestrator:1814) / calculate_finding_confidence(confidence_scorer:317): 死代码, P7 顺手清"
sync_status: "not_synced"
last_check_at: "2026-06-20"
previous_goal:
  goal_id: "phase17-ai-static-deepening"
  title: "Phase 17 — AI 与静态最优结合的深化"
  status: "completed"
  commit_id: "fca0c22"
