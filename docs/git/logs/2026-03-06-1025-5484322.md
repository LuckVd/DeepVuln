# 5484322

## 基本信息

| 字段 | 值 |
|------|-----|
| **时间** | 2026-03-06 10:25 |
| **Commit** | 54843225e90964d112643e2dd916808ccd852d22 |
| **Message** | feat(l3): integrate CodeQL dataflow results into exploitability assessment (P5-01a) |

## 简介

P5-01a: 可利用性评估增强 - CodeQL 数据流整合。将 CodeQL 扫描的数据流分析结果整合到 Round 4 可利用性评估中，优先使用 CodeQL 确认的污点路径来评估漏洞可利用性。

## 变更文件

| 文件 | 操作 |
|------|------|
| `src/layers/l3_analysis/rounds/round_four.py` | 修改 |
| `src/cli/main.py` | 修改 |
| `docs/CURRENT_GOAL.md` | 修改 |
| `docs/ROADMAP.md` | 修改 |
| `docs/git/history.md` | 修改 |

## 功能变更

| 功能 | 操作 | 说明 |
|------|------|------|
| CodeQL 数据流整合 | 新增 | RoundFourExecutor 接收 CodeQL 结果 |
| CodeQL 索引构建 | 新增 | 按 file:line 索引 CodeQL findings |
| 数据流信息提取 | 新增 | 从 CodeQL metadata 提取 source/sink/sanitizers |
| 可利用性评估增强 | 修改 | 优先使用 CodeQL 确认的数据流 |
| CLI 传递 CodeQL | 新增 | 收集 CodeQL 结果传递给 Round 4 |

## 核心算法

### 可利用性判断优先级

```
1. CodeQL confirmed taint flow     → EXPLOITABLE (0.90)
2. CodeQL with sanitizers         → CONDITIONAL (0.70)
3. CodeQL user source only        → UNLIKELY (0.45)
4. Static analysis fallback       → 原有逻辑
```

### 数据流信息提取

```python
def _extract_codeql_dataflow_info(finding):
    # 从 metadata.data_flow 提取：
    - source_type: user_input | config | trusted | internal
    - sink_type: sql_query | html_output | command_exec | ...
    - sanitizers: [escape_html, parameterized_query, ...]
    - source_description: "request.getParameter('id')"
    - sink_description: "executeQuery(sql)"
```

## 关联目标

- 目标： P5-01a：整合 CodeQL 数据流结果
- 状态： in_progress
- 父任务： P5-01：可利用性评估增强

## 测试结果

```
28 passed, 1 warning
- CodeQL index building: ✓
- Dataflow extraction: ✓
- Exploitability assessment: ✓
```

## CodeQL 升级

同时完成了 CodeQL CLI 升级：
- 版本： 2.20.1 → 2.20.5
- 语言包： JavaScript (2.0.0), Python (1.7.8), Java (1.10.8), Go (1.5.7), C++ (1.5.12), Ruby (1.5.8), C# (1.6.3)
- 总大小： ~399MB
